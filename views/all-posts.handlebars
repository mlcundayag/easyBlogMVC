<div class="row">
  <div class="col-auto">
    {{!-- this can not be displayed somehow --}}
  <h2>Hi {{user.username}}! </h2>
  </div>
</div>
<br>
{{#if allPost.length}}
<div class="card flex-column">
  {{#each posts as |post|}}
  {{> edit-Item post}}
  {{/each}}
</div>
{{/if}}