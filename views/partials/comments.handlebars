<div class="comments">
  {{#each this}}
  <div class="comment-card">
    <p>
    {{commentContent}} - {{user.username}} {{format_date createdAt}}:{{format_time createdAt}}
    </p>
  </div>
  {{/each}}
</div>